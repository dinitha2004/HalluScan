# HallKing Colab serving extras — installed by notebooks/7_backend_colab.ipynb ON TOP of
# Colab's preinstalled CUDA torch + that notebook's explicit ML line (transformers, accelerate,
# bitsandbytes, sentencepiece, scikit-learn, pandas, pyarrow).
# Do NOT `pip install -r requirements.txt` on Colab — it reinstalls torch and breaks CUDA.
fastapi
uvicorn[standard]
pyngrok
pysbd
nest_asyncio
requests
